\* Required finite profile for active reuse, gallery switching, source
\* mutation, boundary rejection, and fixed-audit rebuild rejection.  This is
\* not a Python/SQLite/filesystem refinement or an unbounded proof.
SPECIFICATION Spec

CONSTANT Galleries = {g1, g2}

INVARIANTS
    TypeOK
    ActiveCacheContainsAtMostOneGallery
    ReturnedPagePassedCurrentBoundaryAudit
    ChangedActiveGalleryFailsClosed
    ChangedFixedAuditCannotBeReactivated
    RejectedBuildPreservesPreviousActive
